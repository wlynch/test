name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo  ${MY_ENV_VAR}
    env:
      MY_ENV_VAR: ${{ startsWith(github.event_name, 'pull_request') refs/pull/github.event.number/merge }} || github.ref }}
